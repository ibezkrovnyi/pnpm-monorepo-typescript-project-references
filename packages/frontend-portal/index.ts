import { answer } from 'frontend'

const finalAnswer: number = answer;

const text = `Answer to the Ultimate Question of Life, the Universe, and Everything is ${finalAnswer}`;

console.log('frontend-portal', text);
